// Generated by tools/generate-cli-options.mjs from ATLAS_CONFIGURATION_SCHEMA. Do not edit.
//
// The option set atlas init accepts is the configuration schema rather than a list that agrees
// with it, so a key added to the parser is settable on the day it lands. pnpm run
// verify:cli-options fails when this file and the schema have drifted apart.

/** What a flag does with the value beside it. */
export type AtlasInitOptionShape = 'scalar' | 'list' | 'map';

/** The scalar types the init flag conventions cover. A schema leaf outside these fails the build. */
export type AtlasInitLeafType = 'string' | 'number' | 'boolean';

/** Every configuration key an invocation can set. */
export type AtlasInitOptionKey =
  | 'sourceLocale'
  | 'defaultLocale'
  | 'locales'
  | 'personNameLocales'
  | 'pseudoLocales'
  | 'formatting'
  | 'inProgress'
  | 'parentLocales'
  | 'aliases';

/** One flag, and everything the parser and the help text need to know about it. */
export interface AtlasInitOptionDescriptor {
  /** The configuration key this flag sets, spelled as atlas.config.json spells it. */
  readonly key: AtlasInitOptionKey;
  readonly flag: string;
  /** The value placeholder --help prints beside the flag. */
  readonly value: string;
  /** The schema description, printed by --help and shipped in the JSON Schema. */
  readonly description: string;
  readonly shape: AtlasInitOptionShape;
  /** Path segments beneath the key: 0 for a scalar or list, 1 for a map of scalars, 2 for a map of objects. */
  readonly depth: number;
  /** The value type at the end of the path. Not consulted at depth 2, where `options` carries each type. */
  readonly leaf: AtlasInitLeafType;
  /** For a depth-2 map, the option names beneath an entry and their types. Empty otherwise. */
  readonly options: Readonly<Record<string, AtlasInitLeafType>>;
  /** The configuration schema requires this key, so an invocation that omits it cannot be completed. */
  readonly required: boolean;
  /** The flag may appear more than once, each occurrence adding rather than replacing. */
  readonly repeatable: boolean;
}

/** Required first, then schema order. */
// prettier-ignore
export const ATLAS_INIT_OPTIONS: readonly AtlasInitOptionDescriptor[] = Object.freeze([
  {
    key: 'sourceLocale',
    flag: '--source-locale',
    value: "<locale>",
    description: "The locale the authored source catalogs are written in.",
    shape: 'scalar',
    depth: 0,
    leaf: 'string',
    options: Object.freeze({}),
    required: true,
    repeatable: false,
  },
  {
    key: 'defaultLocale',
    flag: '--default-locale',
    value: "<locale>",
    description: "The locale served when a request asks for none Atlas supports.",
    shape: 'scalar',
    depth: 0,
    leaf: 'string',
    options: Object.freeze({}),
    required: true,
    repeatable: false,
  },
  {
    key: 'locales',
    flag: '--locale',
    value: "<locale>",
    description: "Every locale this project supports. Repeat for each one.",
    shape: 'list',
    depth: 0,
    leaf: 'string',
    options: Object.freeze({}),
    required: true,
    repeatable: true,
  },
  {
    key: 'personNameLocales',
    flag: '--person-name-locale',
    value: "<locale>",
    description: "Locales a person name may be written in, beyond the ones this project is translated into.",
    shape: 'list',
    depth: 0,
    leaf: 'string',
    options: Object.freeze({}),
    required: false,
    repeatable: true,
  },
  {
    key: 'pseudoLocales',
    flag: '--pseudo-locale',
    value: "<locale>[.<option>=<value>]",
    description: "Locales derived from the source catalog rather than authored. Generated only by --pseudo.",
    shape: 'map',
    depth: 2,
    leaf: 'string',
    options: Object.freeze({"lengthFactor":"number","markers":"boolean"}),
    required: false,
    repeatable: true,
  },
  {
    key: 'formatting',
    flag: '--formatting',
    value: "<locale>.<option>=<value>",
    description: "How a locale is written, where the application disagrees with CLDR about that locale.",
    shape: 'map',
    depth: 2,
    leaf: 'string',
    options: Object.freeze({"numberingSystem":"string","calendar":"string","hourCycle":"string"}),
    required: false,
    repeatable: true,
  },
  {
    key: 'inProgress',
    flag: '--in-progress',
    value: "<locale>.<option>=<value>",
    description: "Locales still being translated. Their completeness findings never block a release.",
    shape: 'map',
    depth: 2,
    leaf: 'string',
    options: Object.freeze({"note":"string"}),
    required: false,
    repeatable: true,
  },
  {
    key: 'parentLocales',
    flag: '--parent-locale',
    value: "<locale>=<locale>",
    description: "Which locale a locale inherits from, where CLDR is wrong for this project. `und` means none.",
    shape: 'map',
    depth: 1,
    leaf: 'string',
    options: Object.freeze({}),
    required: false,
    repeatable: true,
  },
  {
    key: 'aliases',
    flag: '--alias',
    value: "<alias>=<locale>",
    description: "Extra identifiers that resolve to a supported locale.",
    shape: 'map',
    depth: 1,
    leaf: 'string',
    options: Object.freeze({}),
    required: false,
    repeatable: true,
  },
]);

/** The "Init options:" block of atlas --help, wrapped to a width a terminal has. */
// prettier-ignore
export const ATLAS_INIT_HELP = "Init options:\n  --source-locale <locale>\n      The locale the authored source catalogs are written in.\n  --default-locale <locale>\n      The locale served when a request asks for none Atlas supports.\n  --locale <locale>   (repeatable)\n      Every locale this project supports. Repeat for each one.\n  --person-name-locale <locale>   (repeatable)\n      Locales a person name may be written in, beyond the ones this project is translated into.\n  --pseudo-locale <locale>[.<option>=<value>]   (repeatable)\n      Locales derived from the source catalog rather than authored. Generated only by --pseudo.\n      lengthFactor (number)\n        Signed length change. Positive expands, negative elides vowels; absent leaves length alone.\n      markers (boolean)\n        Wrap the whole rendered message in boundary markers, so untranslated text has none.\n  --formatting <locale>.<option>=<value>   (repeatable)\n      How a locale is written, where the application disagrees with CLDR about that locale.\n      numberingSystem (string)\n        The digits this locale is written in, where they differ from the ones CLDR gives it.\n      calendar (string)\n        The calendar this locale is written in, where it differs from the one CLDR gives it.\n      hourCycle (string)\n        The hour cycle this locale is written in, where it differs from the one CLDR gives it.\n  --in-progress <locale>.<option>=<value>   (repeatable)\n      Locales still being translated. Their completeness findings never block a release.\n      note (string)\n        Why this locale is not complete yet. Quoted back in every finding it downgrades.\n  --parent-locale <locale>=<locale>   (repeatable)\n      Which locale a locale inherits from, where CLDR is wrong for this project. `und` means none.\n  --alias <alias>=<locale>   (repeatable)\n      Extra identifiers that resolve to a supported locale.\n\n  A repeatable flag may appear more than once. Where a value contains \"=\", the left side is a\n  path beneath the field: \"eg=ar-EG\" sets one entry, \"en-XA.markers=true\" sets one option of\n  one entry. Every field atlas.config.json accepts is above; there is no other way to set one.";

/** The complete invocation printed beside ATL1701. Every settable field, required ones first. */
// prettier-ignore
export const ATLAS_INIT_INVOCATION = "atlas init --source-locale <locale> --default-locale <locale> --locale <locale> [--locale <locale> ...] [--person-name-locale <locale> ...] [--pseudo-locale <locale>[.<option>=<value>] ...] [--formatting <locale>.<option>=<value> ...] [--in-progress <locale>.<option>=<value> ...] [--parent-locale <locale>=<locale> ...] [--alias <alias>=<locale> ...]";
