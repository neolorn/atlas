# Atlas standards sources

The consumer configuration profile `atlas-1` resolves to the exact standards and source-data selection in `sources.lock.json`. Consumers select the Atlas profile; they do not maintain Unicode, CLDR, IANA, or XLIFF versions themselves. Section 1 of `specs/01-standards-profile.spec.md` declares that profile, and section 4 of `specs/01-standards-profile.spec.md` is why every source behind it is recorded here rather than known only to whoever installed it.

Every digest is an explicit `{ algorithm, encoding, value }` object. Hex values are not Subresource Integrity strings.

## Inventory

| Source                                                                | Role and provenance                                                                                                                                                                                                                                                                                               | License or terms                                       | Package boundary                                                                                              | Update and replacement risk                                                                                                                                       |
| --------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Unicode CLDR `core.zip` 48.2                                          | Official locale-data build input and LDML/MessageFormat profile source from the versioned Unicode release directory and checksum manifest.                                                                                                                                                                        | Unicode-3.0                                            | Toolkit/build input only; the archive never enters the browser runtime package.                               | Intentional review only. Data changes can alter formatting, plural, locale, and generated-artifact behavior.                                                      |
| IANA Language Subtag Registry, file date 2026-06-14                   | Official BCP 47 registry input. The rolling upstream file is preserved as the immutable project snapshot named by the lock.                                                                                                                                                                                       | IANA/IETF Protocol Registries public-domain dedication | Toolkit/build input only; only deliberately generated derived data may later enter a runtime profile.         | Intentional review only. Registry updates can change canonicalization and validation.                                                                             |
| Unicode UCD `DerivedBidiClass.txt` 17.0.0, file date 2025-07-24       | Official `Bidi_Class` derivation from the versioned UCD directory, cross-checked byte-for-byte against the normative `UCD.zip`. The strong right-to-left character class is derived from it: `Bidi_Class` answers "does this reorder the text around it" directly, where script membership only stands in for it. | Unicode-3.0                                            | Toolkit/build input only; the generated character class enters the runtime package, the file does not.        | Intentional review only. A new Unicode release can add strong characters, and the generator gate fails until the file and the generated class are moved together. |
| IANA Time Zone Database 2026c                                         | Official versioned time-zone source archive.                                                                                                                                                                                                                                                                      | Public domain                                          | Toolkit/build input only unless a future approved profile explicitly derives bounded runtime data.            | Intentional review only. Political rule changes are expected and can change time-zone behavior.                                                                   |
| OASIS XLIFF 2.2 CS01 schemas, twelve files, file date 2025-03-13      | Normative schemas for the pinned XLIFF profile, acquired from the versioned CS01 stage directory and cross-checked byte-for-byte against the official archive.                                                                                                                                                    | OASIS IPR Policy                                       | Toolkit/gate input only; schemas never enter any shipped package.                                             | Intentional review only. A schema change is a profile change and requires the compatibility review below.                                                         |
| W3C XML namespace schema `xml.xsd`                                    | Imported by the XLIFF core schema for `xml:lang` and `xml:space`. Vendored because the validator makes no network requests, which is what keeps the gate deterministic.                                                                                                                                           | W3C Software and Document Notice and License           | Toolkit/gate input only; sits in the XLIFF schema directory and is the one file there OASIS does not publish. | Stable. A change would come with a new W3C revision, reviewed the same way.                                                                                       |
| sitemaps.org 0.9 schemas, `sitemap.xsd` and `siteindex.xsd`           | Normative schemas for the sitemap protocol Atlas writes, acquired from the publisher's own schema directory. The runtime gate validates every sitemap and index Atlas emits against them.                                                                                                                         | sitemaps.org, unversioned publication                  | Runtime/gate input only; schemas never enter any shipped package.                                             | Intentional review only. The protocol has not moved since 0.9 and a change to it would be a change to what Atlas publishes.                                       |
| W3C XHTML 1.0 Strict schema `xhtml1-strict.xsd`                       | Declares the global `link` element the hreflang extension puts inside a sitemap `url`. Both sitemap schemas close their content models with a `##other` wildcard at `processContents="strict"`, so a validator refuses `xhtml:link` unless this namespace is imported.                                            | W3C Software and Document Notice and License           | Runtime/gate input only; imports the `xml.xsd` already vendored for XLIFF rather than a second copy.          | Stable. A change would come with a new W3C revision, reviewed the same way.                                                                                       |
| `atlas-sitemap-hreflang.xsd`                                          | **Atlas's own file, not a standard.** An import shim and nothing else: it imports the sitemap namespace and the XHTML namespace so a validator compiles both into one schema set. It exists because the strict wildcard above demands a declaration a preloaded file does not supply.                             | Part of Atlas, under the repository license            | Runtime/gate input only.                                                                                      | Changes only when the two schemas it imports do.                                                                                                                  |
| Unicode 17.0.0, LDML/MessageFormat 48.2, and XLIFF 2.2 CS01 documents | Normative behavior references at immutable version/stage URLs.                                                                                                                                                                                                                                                    | Publisher terms linked by the owning documents         | Referenced, not bundled.                                                                                      | A standards upgrade requires compatibility review and a deliberate Atlas profile decision.                                                                        |

`data/sitemaps-0.9-schemas/` mixes three publishers and one file of Atlas's own, and the lock records each separately. `sitemap.xsd` and `siteindex.xsd` are sitemaps.org's; `xhtml1-strict.xsd` is W3C's, vendored because the hreflang extension is Google's and neither publisher's schema imports the other's; `atlas-sitemap-hreflang.xsd` is ours and is marked as ours. That last file is an import and no more. Both sitemap schemas end every content model with `<xsd:any namespace="##other" processContents="strict"/>`, and `strict` means a validator must find a global declaration for `xhtml:link` or reject the document; making the XHTML schema merely findable does not answer it, because the namespace has to be imported into the compiled set. A sitemap index is validated against `siteindex.xsd` directly instead, since it carries no foreign element and the two sitemap schemas share a namespace, so a second import of that namespace is skipped. `xhtml1-strict.xsd` imports the W3C `xml.xsd` vendored beside the XLIFF schemas; one copy serves both sets, and its digest is already recorded there.

One file in `data/xliff-2.2-cs01-schemas/` is not OASIS's: `xml.xsd` is W3C's, and it is vendored alongside the twelve because the core schema imports it and the validator resolves nothing over the network. The lock records the two publishers separately even though the files share a directory. `itsm.xsd` carries an import of `xliff_core_2.0.xsd`, which OASIS does not ship in that directory; nothing Atlas validates reaches it, and the lock says so rather than leaving a reader to discover a dangling reference.

`DerivedBidiClass.txt` is the one file here whose defaults live outside its data lines. Unassigned
code points in blocks reserved for right-to-left scripts take `R` or `AL`, and the file carries that
on `@missing:` lines rather than listing the code points. A reader that parses only the data lines
gets a class that is correct for every character that exists today and wrong for the next one added
to the Arabic block. The generator applies the `@missing` defaults first and lets every explicit
line override them.

## Update policy

A source update is never selected by a floating URL or an install-time network lookup. The maintainer must:

1. select an official immutable release or stage;
2. acquire it from the authoritative publisher;
3. verify byte length and digest against the publisher's checksum when available;
4. preserve a project-owned copy when the authoritative source is rolling;
5. review semantic and licensing changes;
6. regenerate affected fixtures and run the complete Atlas verification suite;
7. change the Atlas profile identifier when compatibility requires consumers to opt into changed behavior.

The project-owned IANA registry snapshot is an authoritative build input only after its file date, byte length, and digest match `sources.lock.json`.
