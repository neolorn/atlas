# Contributing to Atlas

Thank you for your interest in contributing to Atlas. This document explains how the project is organized, how to set up a development environment, and what a contribution needs to look like to be merged.

## Table of contents

- [Code of conduct](#code-of-conduct)
- [Reporting bugs and requesting features](#reporting-bugs-and-requesting-features)
- [Development setup](#development-setup)
- [Running the checks](#running-the-checks)
- [Making a change](#making-a-change)
- [Commit messages](#commit-messages)
- [Pull requests](#pull-requests)
- [Documentation](#documentation)
- [Dependencies](#dependencies)
- [Releases](#releases)

## Code of conduct

This project follows the [Contributor Covenant](CODE_OF_CONDUCT.md). By participating, you agree to uphold it.

## Reporting bugs and requesting features

Use the [issue templates](https://github.com/neolorn/atlas/issues/new/choose). For a bug, include the versions of `@neolorn/atlas` and `@neolorn/atlas-toolkit`, the Angular version, and the smallest reproduction you can provide: the catalog, the configuration, or the route involved.

For a feature, open an issue before writing code. Atlas covers localization in general; a capability that only one application needs belongs in that application, and the issue is where that question is settled.

Do not report security vulnerabilities in public issues. See [SECURITY.md](SECURITY.md).

## Development setup

Atlas is a pnpm workspace with two packages, `packages/runtime` and `packages/toolkit`, and a set of consumer applications under `fixtures/` used for verification.

Requirements:

- Node.js on one of the supported lines listed in [Compatibility](docs/reference/compatibility.md)
- pnpm at the version declared in the `packageManager` field of `package.json`

Install and build:

```sh
pnpm install
pnpm run build
```

The verification suite drives real applications in Chromium, Firefox, and WebKit. Install the browser engines once:

```sh
pnpm exec playwright install chromium firefox webkit
```

## Running the checks

The quick checks run in under a minute and cover formatting, linting, types, and the unit suites of both packages:

```sh
pnpm run format:check
pnpm run lint
pnpm run typecheck
pnpm run test:toolkit
pnpm run test:runtime
```

The full verification suite builds both packages, installs them into consumer applications from the packed tarballs, runs those applications on three Angular versions and three browser engines, executes every code example in the documentation, and checks the generated reference pages against the packages:

```sh
pnpm run verify
```

It takes about fifteen minutes. Run it on the finished change before opening a pull request. If a stage cannot run in your environment, say so in the pull request.

Three commands are maintenance rather than checks:

```sh
pnpm run clean
pnpm run refresh:consumer
pnpm run refresh:consumer-locks
```

`clean` deletes `dist/` and `tmp/`. Run it when a build output from an earlier state of the tree is being read by something that should be reading a fresh one, or to discard the work trees and consumer installs the verification suite leaves behind. `pnpm run clean dist` removes the build output alone.

`refresh:consumer` hands the consumer fixtures the packages that are in `dist/` right now. A fixture takes Atlas as a `file:` dependency, which pnpm copies into its store, so a fixture that is already installed goes on running the build it was installed with until this runs.

`refresh:consumer-locks` regenerates the committed lockfiles of the consumer fixtures. It reaches the network, so run it when a fixture manifest or a dependency range changes and commit what it writes.

## Making a change

The [specifications](specs/README.md) describe what Atlas accepts, produces, and guarantees. Read the specification that owns the area you are changing before you change its behavior. If the specification is wrong, open an issue; the specification is corrected in the same change as the code.

A change is one coherent unit of work. Keep unrelated changes in separate commits and separate pull requests.

Do not commit:

- secrets, credentials, tokens, or local environment values;
- build output, caches, logs, or dependency directories;
- editor or operating system files.

Generated files are committed only where the repository treats them as source, and they are regenerated by the build; do not edit them by hand.

## Commit messages

Commit messages follow [Conventional Commits 1.0.0](https://www.conventionalcommits.org/en/v1.0.0/):

```text
<type>(<scope>): <description>

<body>
```

- `type` is one of `feat`, `fix`, `docs`, `refactor`, `perf`, `test`, `build`, `ci`, `chore`, or `revert`.
- `scope` is optional and names the area, for example `routing`, `toolkit`, or `specs`.
- The description is imperative, lowercase, and has no trailing period.
- The body explains what changed and why when the subject is not enough. A breaking change is described in the body, with the migration.

Examples:

```text
feat(routing): derive locale-neutral roots from the URL policy
fix(toolkit): report the parent scope when a message pointer does not resolve
docs: add the sitemap how-to
chore(release): 1.0.0
```

## Pull requests

- One purpose per pull request.
- The title is a valid commit subject; it becomes the subject of the squash commit.
- The description says what changed, why, how it was verified, and any migration a user needs.
- Link the related issue when there is one.

Pull requests are squash-merged onto `main`, which is the only long-lived branch and stays linear. Name topic branches `<type>/<short-description>`, for example `feat/parent-locale-fallback`.

## Documentation

Documentation lives under `docs/` and follows the [Diátaxis](https://diataxis.fr) structure: a tutorial, how-to guides, reference pages, and explanation pages. Reference pages are generated from the packages and are not edited by hand.

Every code block in the documentation and in the READMEs is executed by the verification suite. A block that names a file is written into a real application; a page that writes a file carries a spec that must pass. Label every block with its destination.

For style, follow the [Google developer documentation style guide](https://developers.google.com/style): second person, present tense, active voice, American spelling.

`CHANGELOG.md` follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/) and [Semantic Versioning](https://semver.org/spec/v2.0.0.html): one section per version, newest first, entries grouped under Added, Changed, Deprecated, Removed, Fixed and Security, and each entry stating what a reader of the library meets.

## Dependencies

Atlas's runtime has no dependencies beyond `tslib`, and the toolkit's dependencies are pinned to exact versions. Adding a dependency, a polyfill, or a use of an undocumented platform or compiler API is a project decision. Open an issue before proposing one.

## Releases

Releases are cut by the maintainers following [RELEASING.md](RELEASING.md). Version numbers follow [Semantic Versioning](https://semver.org); what a version promises is described in [Versioning and compatibility](docs/versioning.md).
